This roll bundles the rapidminer data mining tool.  To install, execute these
instructions on a Rocks development machine (e.g., a frontend), starting in
this directory:

% make
% rocks add roll *.iso
% rocks enable roll rapidminer
% cd /export/rocks/install
% rocks create distro
% rocks run roll rapidminer | bash

In addition to the software itself, the roll installs rapidminer environment
module files in /opt/modulefiles/applications/rapidminer.
